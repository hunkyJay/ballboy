package ballboy.model.observer;
/**
 * The base interface for observers in Observer design pattern.
 */
public interface Observer {
    /**
     * Update the record in the observer.
     */
    void update();
}
